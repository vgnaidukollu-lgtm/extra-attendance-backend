# Used with @ActiveProfiles("test"). Datasource + JPA are overridden in
# AttendanceApiIntegrationTest via @DynamicPropertySource so tests always use H2.

app.jwt.secret=test-jwt-secret-for-integration-tests-only
app.admin.username=admin
app.admin.password=unused-in-test-profile
